import React from 'react';
import './Spinner.css'; // Ensure you include the CSS styles

const Spinner = () => (
  <div className="spinner-overlay">
    <div className="spinner"></div>
  </div>
);

export default Spinner;
